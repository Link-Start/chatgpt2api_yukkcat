# R0/R1 前端只读 Smoke 记录

时间：2026-06-04

范围：只做 R0/R1 动作，也就是打开页面、读取数据、筛选/分页类前端状态检查、截图。不执行导入、删除、保存、刷新全部账号、恢复异常账号、代理测试、WebDAV/R2 测试、图库清理等动作。

## 结果总览

| 页面 | 路由 | 检查点 | 结果 | 截图 |
| --- | --- | --- | --- | --- |
| 概览中心 | `/#/` | 账号卡片、接口信息、6 张模型图表标题 | 通过 | `artifacts/r0r1-smoke-dashboard-20260604.png` |
| 账号管理 | `/#/accounts` | 主表字段、50 条分页、编辑/更多按钮、导入/导出入口 | 通过 | `artifacts/r0r1-smoke-accounts-20260604.png` |
| 日志管理 | `/#/logs` | 统计卡、服务端筛选、500 条分页、查看详情/删除入口 | 通过 | `artifacts/r0r1-smoke-logs-20260604.png` |
| 图片画廊 | `/#/gallery` | 服务端分页、24 张当前页、1021 总量、保留天数口径 | 通过 | `artifacts/r0r1-smoke-gallery-stable-20260604.png` |
| 代理管理 | `/#/proxy` | 全局代理、代理分组统计、新建分组入口 | 通过 | `artifacts/r0r1-smoke-proxy-20260604.png` |
| 系统设置 | `/#/settings` | 基础连接、图片链路、缓存、图片存储、备份、CPA/Sub2API | 通过 | `artifacts/r0r1-smoke-settings-20260604.png` |
| 文档中心 | `/#/docs` | 接口说明、模型列表、风险说明入口 | 通过 | 未新增 |

## 观察

- 控制台无页面 error 日志。
- Dashboard 显示账号总数、正常账号、限流账号、异常账号、禁用账号、剩余图片额度，并保留模型请求分布、调用趋势、成功率趋势、平均响应时间、模型调用占比、模型使用排行。
- 账号管理当前页加载 50 行，按钮包含刷新列表、刷新所有账号信息和额度、恢复异常账号、添加账号、导入账号、导出选中、导出全部、编辑、更多。本次没有点击任何会改数据的按钮。
- 日志管理当前页加载 500 条，服务端命中 676 条，主表显示时间、类型、令牌名称、调用耗时、状态、图片、简述和操作。本次没有删除日志。
- 图片画廊复查后稳定加载 24 张当前页，总量 1021，保留天数显示为“保留 / 天”，倒计时显示为“天/小时”格式。本次没有下载、删除、清理、压缩或改标签。
- 代理管理显示全局代理和代理分组入口，当前代理分组为空。本次没有保存或测试代理。
- 设置页实际标题为“系统设置”，不是脚本里最初写的“设置中心”；功能区加载正常。本次没有保存设置，也没有触发 WebDAV/R2/CPA/Sub2API 测试。
- 2026-06-04 二次只读巡检：Dashboard、Accounts、Logs、Gallery、Proxy、Settings、Docs 均可加载，页面控制台 error 为 0。
- 日志管理已新增“运行日志”只读视图：可显示运行日志统计、级别/来源筛选、空态和候选日志文件说明；不提供删除/清空动作。
- 文档中心默认停留在“接口说明”，运行日志部署说明位于“运维边界”标签；代码和构建均已确认文案为 `/api/runtime-logs` 只读接口。

## 后续

下一步进入 R2 小范围写入 smoke 前，需要先准备明确测试对象：

- 一个可修改但不影响生产调度的测试账号。
- 一张可编辑标签的测试图片。
- 一个可新增/删除的测试代理分组。
- 一组可保存后恢复的设置项。

R3/R4 仍保持禁止默认执行：

- 不导入真实账号。
- 不刷新全部真实账号。
- 不恢复异常账号。
- 不删除真实账号、日志、图片或备份。
- 不执行图库清理、WebDAV/R2 测试、CPA/Sub2API 导入、代理真实连通性测试。
